interface ITriangulationBox {
  setWidth(width: number);
  getWidth();
  setHeight(height: number);
  getHeight();
  setDept(dept: number);
  getDept();
  getTriangles();
  getVertices();
}

export default ITriangulationBox;